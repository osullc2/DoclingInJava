# Step 2: Simple Python Library in Java

This folder will contain a Maven project that demonstrates using a simple Python library (e.g., `math`, `datetime`, or a lightweight third-party library) from Java using GraalVM polyglot.

## Status

🚧 **Coming Soon** - This step will be implemented next.

## Planned Features

- Use a simple Python library (e.g., `math`, `datetime`, `random`)
- Call Python library functions from Java
- Pass data between Java and Python
- Handle return values from Python functions
